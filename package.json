{
  "name": "summarizer",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild sidepanel.js --bundle --outfile=sidepanel.dist.js --format=esm --minify --target=chrome100",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@google/genai": "^1.47.0",
    "esbuild": "^0.27.4"
  }
}
